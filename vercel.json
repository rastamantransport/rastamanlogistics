{
  "buildCommand": "echo skip",
  "outputDirectory": "dist",
  "trailingSlash": false,
  "cleanUrls": true,
  "rewrites": [
    { "source": "/(:path*)", "destination": "/:path*/index.html" }
  ]
}
```

Save, then:
```
git add -A
git commit -m "fix routing with path rewrite"
git push